# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.npm

# Build outputs
dist/
build/
out/
*.tsbuildinfo
.next/

# Testing
coverage/
.nyc_output/
*.lcov

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
*.bak
.DS_Store

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# But we do want the template
!.env.development

# Logs
logs/
*.log

# OS files
Thumbs.db

# Temporary files
*.tmp
*.temp
.tmp/
.temp/

# Git worktrees (they're managed separately)
# The worktrees directory structure is tracked but not the actual worktree checkouts
worktrees/*/*
!worktrees/*/.gitkeep

# MosAIc runtime
.mosaic/cache/
.mosaic/tmp/
.mosaic/.prepared
.mosaic/data/
.mosaic/logs/
.mosaic/claude/
.mosaic/*.pid
.mosaic/dev-session.sh

# Backup files
*.backup
*.old
backups/

# Lock files (submodules manage their own)
# We don't want to track lock files from submodules
mosaic/package-lock.json
mosaic-mcp/package-lock.json
mosaic-dev/package-lock.json
tony/package-lock.json

# But we do want our own lock file
!package-lock.json

# Claude local settings
.claude/settings.local.json